# GENERATED by deploy/scripts/render-deploy.sh — DO NOT EDIT.
# Source: helm chart deploy/helm/cogneva + profiles/k3s-single.yaml
# Change chart templates / values / profile, then re-run render-deploy.sh.
# Source: cogneva/templates/seaweedfs.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seaweedfs-pvc
  namespace: cogneva
  labels:
    app.kubernetes.io/name: seaweedfs
    app.kubernetes.io/component: objectstore
  annotations:
    # 卸载不销毁数据卷：这是永久记忆 Raw 层的权威副本，删掉等于清空记忆。
    helm.sh/resource-policy: keep
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
