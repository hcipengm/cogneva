# GENERATED by deploy/scripts/render-deploy.sh — DO NOT EDIT.
# Source: helm chart deploy/helm/cogneva + profiles/k3s-single.yaml
# Change chart templates / values / profile, then re-run render-deploy.sh.
# Source: cogneva/templates/security-gateway.yaml
# SSH 兜底镜像的存储。刻意**不带** `helm.sh/resource-policy: keep`：这是本 chart
# 里唯一一个内容可以整个从外部真相源（GitHub）重建的卷，标了 keep 只会在反复
# 重装时堆出无主的陈旧镜像。它也不与 cogneva-git-remote-pvc 合并——裸仓是全部
# 进化分支与晋级产物的规范来源，镜像只是网关的一层传输缓存，二者可丢性不同。
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cogneva-git-mirror-pvc
  namespace: cogneva
  labels:
    app.kubernetes.io/name: cogneva
    app.kubernetes.io/instance: cogneva
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/version: "0.5.7"
    app.kubernetes.io/component: security-gateway
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
