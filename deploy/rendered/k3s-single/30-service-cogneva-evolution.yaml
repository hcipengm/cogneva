# GENERATED by deploy/scripts/render-deploy.sh — DO NOT EDIT.
# Source: helm chart deploy/helm/cogneva + profiles/k3s-single.yaml
# Change chart templates / values / profile, then re-run render-deploy.sh.
# Source: cogneva/templates/evolution.yaml
# 进化 Pod 的指标端点。ServiceMonitor 按 Service 标签发现端点，进化负载此前
# 没有 Service，于是它产生的指标（Ralph 循环终止等）在观测面上完全缺席——
# 监控里只有主应用那一半，恰好漏掉承担大部分 decompose 的那一半。
# 沿用 cogneva 名字标签，与主应用共用同一条 ServiceMonitor，不新增抓取配置。
apiVersion: v1
kind: Service
metadata:
  name: cogneva-evolution
  namespace: cogneva
  labels:
    app.kubernetes.io/name: cogneva
    app.kubernetes.io/instance: cogneva
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/version: "0.5.8"
    app.kubernetes.io/component: evolution
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cogneva-evolution
    app.kubernetes.io/component: evolution
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
