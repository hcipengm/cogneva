# GENERATED by deploy/scripts/render-deploy.sh — DO NOT EDIT.
# Source: helm chart deploy/helm/cogneva + profiles/k8s-standard.yaml
# Change chart templates / values / profile, then re-run render-deploy.sh.
# Source: cogneva/templates/webhook-service.yaml
# 平台 webhook 集群入口（GitHub + Gitee 共用）：NodePort 面向集群外平台回调，
# 转发到安全网关 8082 webhook 通道。验签在网关完成，主应用只收网关附内部
# HMAC 签名的转发。GitHub 侧配 /webhooks/github，Gitee 侧配 /webhooks/gitee。
# selector 必须精确指向网关 Pod：webhook 通道只有网关监听，宽泛 selector
# 会被 kube-proxy 随机选中无监听的端点造成 502。
apiVersion: v1
kind: Service
metadata:
  name: cogneva-github-webhook
  namespace: cogneva
  labels:
    app.kubernetes.io/name: cogneva
    app.kubernetes.io/instance: cogneva
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/version: "0.5.7"
    app.kubernetes.io/component: security-gateway
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: cogneva
    app.kubernetes.io/component: security-gateway
  ports:
    - name: webhook
      port: 8082
      targetPort: 8082
      nodePort: 30991
