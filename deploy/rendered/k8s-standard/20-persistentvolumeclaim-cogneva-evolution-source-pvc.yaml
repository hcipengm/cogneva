# GENERATED by deploy/scripts/render-deploy.sh — DO NOT EDIT.
# Source: helm chart deploy/helm/cogneva + profiles/k8s-standard.yaml
# Change chart templates / values / profile, then re-run render-deploy.sh.
# Source: cogneva/templates/evolution.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cogneva-evolution-source-pvc
  namespace: cogneva
  labels:
    app.kubernetes.io/name: cogneva
    app.kubernetes.io/instance: cogneva
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/version: "0.5.7"
    app.kubernetes.io/component: evolution-source
  annotations:
    # 卸载不销毁数据卷：共享 CARGO_TARGET_DIR 在这里，删掉就得从零冷编译。
    helm.sh/resource-policy: keep
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
