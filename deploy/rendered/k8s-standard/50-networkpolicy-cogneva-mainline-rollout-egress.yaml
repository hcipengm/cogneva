# GENERATED by deploy/scripts/render-deploy.sh — DO NOT EDIT.
# Source: helm chart deploy/helm/cogneva + profiles/k8s-standard.yaml
# Change chart templates / values / profile, then re-run render-deploy.sh.
# Source: cogneva/templates/network-policy.yaml
# 主线滚动 Job Pod（新镜像 smoke test + 门禁滚动四部署）：网络面最小化，
# 只准访问 kube-apiserver（kubectl set image / 查 rollout 与 pod 健康）与
# DNS，不需要数据面、网关、registry（构建与 push 都在进化 Pod 完成）。
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: cogneva-mainline-rollout-egress
  namespace: cogneva
  labels:
    app.kubernetes.io/name: cogneva
    app.kubernetes.io/instance: cogneva
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/version: "0.5.7"
    app.kubernetes.io/component: mainline-rollout
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/component: mainline-rollout
  policyTypes:
    - Egress
  egress:
    # 同 evolution 策略：DNAT 后 apiserver 端点是宿主网络节点 IP:6443，
    # 0.0.0.0/0 配合端口钉死覆盖（详见上方 evolution 规则注释）。
    - to:
        - ipBlock:
            cidr: "10.43.0.0/16"
        - ipBlock:
            cidr: "0.0.0.0/0"
      ports:
        - protocol: TCP
          port: 443
        - protocol: TCP
          port: 6443
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
