# GENERATED by deploy/scripts/render-deploy.sh — DO NOT EDIT.
# Source: helm chart deploy/helm/cogneva + profiles/k8s-standard.yaml
# Change chart templates / values / profile, then re-run render-deploy.sh.
# Source: cogneva/templates/network-policy.yaml
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: cogneva-security-gateway
  namespace: cogneva
  labels:
    app.kubernetes.io/name: cogneva
    app.kubernetes.io/instance: cogneva
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/version: "0.5.7"
    app.kubernetes.io/component: security-gateway
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/component: security-gateway
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector: {}
      ports:
        - protocol: TCP
          port: 8080
        - protocol: TCP
          port: 8081
    # webhook 入口（8082）：NodePort 面向集群外平台回调，源 IP 经 SNAT 为节点
    # 地址，无法用 podSelector 限定；平台签名验签在网关完成（未配置 secret 时
    # 一律 503，fail-closed），故可对集群外开放。
    - ports:
        - protocol: TCP
          port: 8082
  egress:
    - {}
