# GENERATED by deploy/scripts/render-deploy.sh — DO NOT EDIT.
# Source: helm chart deploy/helm/cogneva + profiles/k3s-multi.yaml
# Change chart templates / values / profile, then re-run render-deploy.sh.
# Source: cogneva/templates/evolution.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: cogneva-evolution
  namespace: cogneva
  labels:
    app.kubernetes.io/name: cogneva
    app.kubernetes.io/instance: cogneva
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/version: "0.5.7"
    app.kubernetes.io/component: evolution
rules:
  # 主线跟踪部署器 / image 滚动：四个 cogneva deployment 的具名读与补丁
  # （kubectl set image = PATCH 具名 deployment）。
  - apiGroups: ["apps"]
    resources: ["deployments"]
    resourceNames:
      - "cogneva"
      - "cogneva-security-gateway"
      - "cogneva-sandbox-executor"
      - "cogneva-evolution"
    verbs: ["get", "patch", "update"]
  # list/watch 是集合级操作，RBAC 不能用 resourceNames 收窄，单独一条。
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["list", "watch"]
  # Pod 健康检查（name+component 双标签选择器，集合 list）。
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]
  # 主线滚动 Job：构建侧 apply(create) 派发、状态查询；Job 内滚动复用同一 SA。
  - apiGroups: ["batch"]
    resources: ["jobs"]
    verbs: ["create", "get", "list", "watch", "patch", "update"]
  - apiGroups: [""]
    resources: ["configmaps"]
    resourceNames: ["cogneva-config"]
    verbs: ["get"]
