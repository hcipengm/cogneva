# GENERATED by deploy/scripts/render-deploy.sh — DO NOT EDIT.
# Source: helm chart deploy/helm/cogneva + profiles/k3s-multi.yaml
# Change chart templates / values / profile, then re-run render-deploy.sh.
# Source: cogneva/templates/app-config.yaml
# 主应用统一配置文件（cogneva.json）：经 /etc/cogneva 目录挂载，env 层覆盖其上。
# 内容取自 chart files/，.Files.Get 原样输出；文件内的双花括号占位符属于
# 应用自身的 prompt 模板，不经过 Helm 渲染。
apiVersion: v1
kind: ConfigMap
metadata:
  name: cogneva-json
  namespace: cogneva
  labels:
    app.kubernetes.io/name: cogneva
    app.kubernetes.io/instance: cogneva
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/version: "0.5.7"
    app.kubernetes.io/component: config
data:
  cogneva.json: |-
    {
      "_comment": "Cogneva unified configuration. Place this file at /etc/cogneva/cogneva.json or set COGNEVA_CONFIG_PATH env.",
      "system": {
        "event_channel_capacity": 1024,
        "task_event_channel_capacity": 256,
        "shutdown_timeout_ms": 30000,
        "timeout_checker_interval_secs": 30,
        "stale_task_detector_poll_secs": 15,
        "tool_timeout_secs": 30,
        "sandbox_executor_url": "http://cogneva-sandbox-executor:9090",
        "require_tool_identity": false,
        "skill_hot_reload_interval_secs": 30,
        "grpc_reconnect_interval_secs": 5,
        "probe_timeout_secs": 5,
        "http_timeout_secs": 10,
        "pattern_db_max_size": 256,
        "pattern_max_age_days": 30,
        "pg_max_connections": 16,
        "pg_min_connections": 1,
        "pg_acquire_timeout_secs": 10,
        "pg_idle_timeout_secs": 600,
        "memory_message_broadcast_capacity": 1024,
        "observability_event_channel_capacity": 256,
        "websocket_event_cache_capacity": 1000,
        "anthropic_default_max_tokens": 4096,
        "strict_persistence": true,
        "vector_backend_required": true,
        "_comment_plugin_filtering": "Plugin loading control. enabled_plugins = whitelist (omit for all). disabled_plugins = blacklist (applied when enabled_plugins is omitted).",
        "disabled_plugins": []
      },
      "env": {
        "_comment": "Environment-variable → config-path mappings. Keys are env-var names, values are dot-separated JSON paths inside this file.",
        "COGNEVA_APP_NAME": "app.name",
        "COGNEVA_APP_VERSION": "app.version",
        "COGNEVA_LOG_LEVEL": "app.log_level",
        "COGNEVA_DATA_DIR": "app.data_dir",
        "COGNEVA_CONFIG_DIR": "app.config_dir",
        "COGNEVA_APP_DIR": "app.app_dir",
        "COGNEVA_DB_PROVIDER": "providers.db.provider",
        "COGNEVA_PG_PROVIDER": "providers.pg.provider",
        "COGNEVA_VECTOR_PROVIDER": "providers.vector.provider",
        "COGNEVA_MEDIA_PROVIDER": "providers.media.provider",
        "COGNEVA_STORAGE_PROVIDER": "providers.storage.provider",
        "COGNEVA_WIKI_PROVIDER": "providers.wiki.provider",
        "COGNEVA_REDIS_URL": "dag_executor.redis_url",
        "COGNEVA_WORKSPACE_ID": "dag_executor.workspace_id",
        "COGNEVA_CONSUMER_GROUP": "dag_executor.consumer_group",
        "COGNEVA_READY_TASK_POLL_INTERVAL_SECS": "dag_executor.ready_task_poll_interval_secs",
        "COGNEVA_BATCH_PERSISTENCE_ENABLED": "dag_executor.batch_persistence_enabled",
        "COGNEVA_BATCH_PERSISTENCE_MAX_CHANGES": "dag_executor.batch_persistence_max_changes",
        "COGNEVA_BATCH_PERSISTENCE_INTERVAL_SECS": "dag_executor.batch_persistence_interval_secs",
        "COGNEVA_DECOMPOSITION_MAX_ATTEMPTS": "dag_executor.decomposition_max_attempts",
        "COGNEVA_DECOMPOSITION_ORPHAN_WATCH_ENABLED": "dag_executor.decomposition_orphan_watch_enabled",
        "COGNEVA_DECOMPOSITION_ORPHAN_POLL_INTERVAL_SECS": "dag_executor.decomposition_orphan_poll_interval_secs",
        "COGNEVA_DECOMPOSITION_ORPHAN_STALL_AFTER_SECS": "dag_executor.decomposition_orphan_stall_after_secs",
        "COGNEVA_DECOMPOSITION_ORPHAN_ALERT_DWELL_SECS": "dag_executor.decomposition_orphan_alert_dwell_secs",
        "COGNEVA_RESULT_CLAIM_IDLE_SECS": "dag_executor.result_claim_idle_secs",
        "COGNEVA_RESULT_CLAIM_INTERVAL_SECS": "dag_executor.result_claim_interval_secs",
        "COGNEVA_RESULT_CLAIM_BATCH": "dag_executor.result_claim_batch",
        "COGNEVA_HTTP_PORT": "gateway.http_port",
        "COGNEVA_WS_PORT": "gateway.ws_port",
        "COGNEVA_METRICS_PORT": "gateway.metrics_port",
        "COGNEVA_RAW_LOGGER_ENABLED": "raw_logger.enabled",
        "COGNEVA_RAW_LOGGER_BASE_DIR": "raw_logger.base_dir",
        "COGNEVA_RAW_LOGGER_BUFFER_SIZE": "raw_logger.max_buffer_size",
        "COGNEVA_RAW_LOGGER_FLUSH_INTERVAL_SECS": "raw_logger.flush_interval_secs",
        "COGNEVA_METRICS_SAMPLE_RETENTION_SECS": "metrics.sample_retention_secs",
        "COGNEVA_METRICS_SAMPLE_RETENTION_SWEEP_INTERVAL_SECS": "metrics.sample_retention_sweep_interval_secs",
        "COGNEVA_MEMORY_ENABLED": "memory.enabled",
        "COGNEVA_MEMORY_BACKEND_TYPE": "memory.backend_type",
        "COGNEVA_MEMORY_EMBEDDING_DIMENSION": "memory.embedding_dimension",
        "COGNEVA_MEMORY_AUTO_INGEST": "memory.auto_ingest",
        "COGNEVA_TIER_MIGRATOR_ENABLED": "tier_migrator.enabled",
        "COGNEVA_TIER_HOT_DURATION_SECS": "tier_migrator.hot_duration_secs",
        "COGNEVA_TIER_WARM_DURATION_SECS": "tier_migrator.warm_duration_secs",
        "COGNEVA_TIER_SCAN_INTERVAL_SECS": "tier_migrator.scan_interval_secs",
        "COGNEVA_TIER_COLD_KEY_PREFIX": "tier_migrator.cold_key_prefix",
        "COGNEVA_TIER_TRACE_SCAN_BATCH": "tier_migrator.trace_scan_batch",
        "COGNEVA_METRICS_ENABLED": "metrics.enabled",
        "COGNEVA_SUPERVISOR_HEALTH_INTERVAL_SECS": "supervisor.health_interval_secs",
        "COGNEVA_SUPERVISOR_QUOTA_INTERVAL_SECS": "supervisor.quota_interval_secs",
        "COGNEVA_SUPERVISOR_CONTROL_PLANE_INTERVAL_SECS": "supervisor.control_plane_interval_secs",
        "COGNEVA_SUPERVISOR_CONTROL_PLANE_URL": "supervisor.control_plane_url",
        "COGNEVA_HOOK_ENGINE_DEDUP_WINDOW_SECS": "hook_engine.dedup_window_secs",
        "COGNEVA_AGENT_LOOP_MAX_ITERATIONS": "agent_loop.max_iterations",
        "COGNEVA_AGENT_LOOP_CONTEXT_WINDOW_SIZE": "agent_loop.context_window_size",
        "COGNEVA_AGENT_LOOP_ROLE": "agent_loop.role",
        "COGNEVA_AGENT_LOOP_SKILL_CACHE_TTL_SECS": "agent_loop.skill_cache_ttl_secs",
        "COGNEVA_SELF_REVIEW_ENABLED": "self_review.enabled",
        "COGNEVA_SELF_REVIEW_MAX_ITERATIONS": "self_review.max_iterations",
        "COGNEVA_SELF_REVIEW_QUALITY_THRESHOLD": "self_review.quality_threshold",
        "COGNEVA_SELF_EVOLUTION_ENABLED": "self_evolution.enabled",
        "COGNEVA_SELF_EVOLUTION_AUTO_APPLY": "self_evolution.auto_apply",
        "COGNEVA_SELF_EVOLUTION_AUTO_DEPLOY": "self_evolution.auto_deploy",
        "COGNEVA_SELF_EVOLUTION_SANDBOX_MODE": "self_evolution.sandbox_mode",
        "COGNEVA_SELF_EVOLUTION_FORCE_AUTONOMOUS": "self_evolution.force_autonomous",
        "COGNEVA_SELF_EVOLUTION_MANUAL_APPROVE": "self_evolution.manual_approve",
        "COGNEVA_SELF_EVOLUTION_SWITCH_MODE": "self_evolution.switch_mode",
        "COGNEVA_SELF_EVOLUTION_CHANGE_DIR": "self_evolution.change_dir",
        "COGNEVA_SELF_EVOLUTION_BINARY_DIR": "self_evolution.binary_dir",
        "COGNEVA_SELF_EVOLUTION_BACKUP_DIR": "self_evolution.backup_dir",
        "COGNEVA_SELF_EVOLUTION_POLL_INTERVAL_SECS": "self_evolution.poll_interval_secs",
        "COGNEVA_SELF_EVOLUTION_SCHEMA_REPAIR_INTERVAL_SECS": "self_evolution.schema_repair_interval_secs",
        "COGNEVA_SELF_EVOLUTION_TEST_TIMEOUT_SECS": "self_evolution.test_timeout_secs",
        "COGNEVA_SELF_EVOLUTION_BUILD_TIMEOUT_SECS": "self_evolution.build_timeout_secs",
        "COGNEVA_TOOL_TIMEOUT_SECS": "system.tool_timeout_secs",
        "COGNEVA_SANDBOX_EXECUTOR_URL": "system.sandbox_executor_url",
        "COGNEVA_REQUIRE_TOOL_IDENTITY": "system.require_tool_identity",
        "COGNEVA_SKILL_HOT_RELOAD_INTERVAL_SECS": "system.skill_hot_reload_interval_secs",
        "COGNEVA_GRPC_RECONNECT_INTERVAL_SECS": "system.grpc_reconnect_interval_secs",
        "COGNEVA_PROBE_TIMEOUT_SECS": "system.probe_timeout_secs",
        "COGNEVA_HTTP_TIMEOUT_SECS": "system.http_timeout_secs",
        "COGNEVA_PATTERN_DB_MAX_SIZE": "system.pattern_db_max_size",
        "COGNEVA_PATTERN_MAX_AGE_DAYS": "system.pattern_max_age_days",
        "COGNEVA_PG_MAX_CONNECTIONS": "system.pg_max_connections",
        "COGNEVA_PG_MIN_CONNECTIONS": "system.pg_min_connections",
        "COGNEVA_PG_ACQUIRE_TIMEOUT_SECS": "system.pg_acquire_timeout_secs",
        "COGNEVA_PG_IDLE_TIMEOUT_SECS": "system.pg_idle_timeout_secs",
        "COGNEVA_MEMORY_MESSAGE_BROADCAST_CAPACITY": "system.memory_message_broadcast_capacity",
        "COGNEVA_OBSERVABILITY_EVENT_CHANNEL_CAPACITY": "system.observability_event_channel_capacity",
        "COGNEVA_WEBSOCKET_EVENT_CACHE_CAPACITY": "system.websocket_event_cache_capacity",
        "COGNEVA_ANTHROPIC_DEFAULT_MAX_TOKENS": "system.anthropic_default_max_tokens",
        "COGNEVA_WEBSOCKET_TIMEOUT_SECS": "gateway.websocket_timeout_secs",
        "COGNEVA_WEBSOCKET_INACTIVITY_TIMEOUT_SECS": "gateway.websocket_inactivity_timeout_secs",
        "COGNEVA_WEBSOCKET_TICK_SECS": "gateway.websocket_tick_secs",
        "COGNEVA_NOTIFICATION_LIMIT": "gateway.notification_limit",
        "COGNEVA_SANDBOX_TASK_TIMEOUT_SECS": "gateway.sandbox_task_timeout_secs",
        "COGNEVA_REQUEST_TIMEOUT_SECS": "gateway.request_timeout_secs",
        "COGNEVA_CONTRIBUTION_OAUTH_REFRESH_INTERVAL_SECS": "gateway.contribution_oauth_refresh_interval_secs",
        "COGNEVA_CONTRIBUTION_OAUTH_REFRESH_THRESHOLD_SECS": "gateway.contribution_oauth_refresh_threshold_secs",
        "COGNEVA_NOTIFICATION_WEBHOOK_URL": "gateway.notification_webhook_url",
        "COGNEVA_STREAM_CAPACITY": "tuning.stream_capacity",
        "COGNEVA_HIGH_WATERMARK": "tuning.high_watermark",
        "COGNEVA_LOW_WATERMARK": "tuning.low_watermark",
        "COGNEVA_MAX_SUMMARIES": "tuning.max_summaries",
        "COGNEVA_STRICT_PERSISTENCE": "system.strict_persistence",
        "COGNEVA_VECTOR_BACKEND_REQUIRED": "system.vector_backend_required"
      },
      "app": {
        "name": "cogneva",
        "version": "0.1.0",
        "log_level": "info",
        "data_dir": "/var/lib/cogneva-data",
        "config_dir": "/etc/cogneva",
        "app_dir": "/opt/cogneva"
      },
      "llm_routing": {
        "_comment": "多 LLM 故障转移。api_style 声明兼容的 API 风格：openai 或 anthropic。默认 openai。",
        "strategy": "failover",
        "retry_on_429": true,
        "retry_on_402": true,
        "max_failover_attempts": 3,
        "backends": [
          {
            "provider": "gateway",
            "_comment": "主应用与沙盒一致零凭证零上游知识：api_key 是写死的非敏感占位串（后端构建要求非空），流量经安全网关 8081 透传，真 key 只在网关 Pod。api_style 钉 openai 匹配透传面；model 同样是占位，网关透传时改写为向导配置的真实模型。多 LLM 故障转移（429/402 切上游）由安全网关的上游池承担，主应用这里只保留网关一个后端。LLM 平等：业务侧配置不出现任何厂商名。",
            "api_key": "via-security-gateway",
            "base_url": "http://cogneva-security-gateway:8081/v1",
            "model": "via-security-gateway",
            "api_style": "openai",
            "weight": 1,
            "enabled": true
          }
        ]
      },
      "providers": {
        "db": {
          "provider": "mysql",
          "enabled": false,
          "options": {
            "host": "mysql.cogneva.svc.cluster.local",
            "port": 3306,
            "database": "cogneva",
            "user": "cog_user",
            "password": "${DB_PASSWORD}"
          }
        },
        "pg": {
          "provider": "postgres",
          "enabled": true,
          "options": {
            "connection": "postgres://cog_user:${PG_PASSWORD}@postgres.cogneva.svc.cluster.local:5432/cogneva",
            "host": "postgres.cogneva.svc.cluster.local",
            "port": 5432,
            "database": "cogneva",
            "user": "cog_user",
            "password": "${PG_PASSWORD}"
          }
        },
        "vector": {
          "provider": "qdrant",
          "enabled": true,
          "options": {
            "url": "http://qdrant.cogneva.svc.cluster.local:6334",
            "collection": "summaries",
            "api_key": null
          }
        },
        "media": {
          "provider": "local-sfu",
          "enabled": false,
          "options": {}
        },
        "storage": {
          "provider": "seaweedfs",
          "enabled": true,
          "options": {
            "endpoint": "http://seaweedfs.cogneva.svc.cluster.local:8333",
            "bucket": "cogneva",
            "region": "us-east-1",
            "access_key": "${S3_ACCESS_KEY}",
            "secret_key": "${S3_SECRET_KEY}",
            "base_path": "/var/lib/cogneva-data/storage"
          }
        },
        "wiki": {
          "provider": "meilisearch",
          "enabled": true,
          "options": {
            "host": "http://meilisearch.cogneva.svc.cluster.local:7700",
            "api_key": "${MEILI_MASTER_KEY}",
            "index": "wiki"
          }
        }
      },
      "dag_executor": {
        "redis_url": "redis://:${REDIS_PASSWORD}@redis-0.redis.cogneva.svc.cluster.local:6379",
        "nats": {
          "urls": [],
          "auth": {
            "username": null,
            "password": null,
            "token": null
          },
          "tls": {
            "enabled": false,
            "ca_cert_path": null,
            "client_cert_path": null,
            "client_key_path": null,
            "insecure_skip_verify": false
          }
        },
        "workspace_id": "default",
        "consumer_group": "cogneva",
        "max_retries": 3,
        "retry_delay_ms": 1000,
        "ready_task_poll_interval_secs": 5,
        "batch_persistence_enabled": false,
        "batch_persistence_max_changes": 10,
        "batch_persistence_interval_secs": 5,
        "archive_enabled": true,
        "archive_after_secs": 3600,
        "archive_poll_interval_secs": 300,
        "result_claim_idle_secs": 600,
        "result_claim_interval_secs": 60,
        "result_claim_batch": 16
      },
      "gateway": {
        "http_port": 8080,
        "ws_port": 8081,
        "metrics_port": 9090,
        "cors_origins": [
          "*"
        ],
        "websocket_timeout_secs": 30,
        "websocket_inactivity_timeout_secs": 90,
        "websocket_tick_secs": 5,
        "notification_limit": 50,
        "sandbox_task_timeout_secs": 30,
        "request_timeout_secs": 30,
        "contribution_oauth_refresh_interval_secs": 3600,
        "contribution_oauth_refresh_threshold_secs": 14400,
        "notification_webhook_url": null
      },
      "metrics": {
        "enabled": true,
        "endpoint": "/metrics",
        "interval_secs": 15,
        "_comment_sample_retention_secs": "cog_metrics_samples 是只追加的观测样本日志，这条是它的保留窗（秒），0=永不清理。读侧全是短窗——抓取读直方图 300 秒、仪表 3600 秒，admin 接口默认 60 秒，而计数器本来就不读这张表（走 cog_metric_counter_totals），所以超出最宽读窗的行任何代码路径都取不回来，只占磁盘。默认一整天：比最宽读窗大一个数量级以上，又与摄取对账的回看窗同长，读指标窗和读摄取窗看的是同一段。属主是主应用：两个 Pod 共用同一个库，进化 Pod 的部署环境里置 COGNEVA_METRICS_SAMPLE_RETENTION_SECS=0，本进程就不参与清理。",
        "sample_retention_secs": 86400,
        "_comment_sample_retention_sweep_interval_secs": "清理扫描间隔（秒）。每轮扫描同时重报这张表的大小，所以它必须显著小于抓取侧的仪表回看窗，",
        "sample_retention_sweep_interval_secs": 600,
        "_comment_scrape_dimensions": "抓取端点向各 observable 询取的维度集合。observable 只回答它按维度分支的那几个维度，所以这里的集合少一个维度，就有一批指标被计数、被谁都不导出——不报错、不打日志，只是没有任何端点能读到。写死成字面量时它是一处不可评审的常量，故改为配置面。准入原则：只收声明了有界序列的维度。D4（memory）、D5（observability）、D6（guardrail）、D9（llm）四家的指标都不带标签，基数固定；D8（collab/orchestrator）已在采集内。D1/D2/D3（agent）在那个分支里逐 step record，身份键取自 task_id，序列基数无界，故不纳入。",
        "scrape_dimensions": ["D4", "D5", "D6", "D8", "D9"]
      },
      "raw_logger": {
        "_comment": "这里写的已是部署的生效值，不再由 cogneva-config 的同名 env 覆盖：同一个设置在两个地方各写一份，读配置文件的人就推不出 runtime 用的是哪份，而 base_dir 还兼着 trace 存储的根（<base_dir>/traces），改错了不报错，只把 trace 写到另一个目录去。env 映射仍留在本文件的 env 段，要按环境改动时走那里。",
        "_comment_format": "proto：记录按长度分隔的 protobuf 落地（<stream>/<YYYY-MM-DD>.proto.bin），比 jsonl 省去字段名开销，也是分层迁移压缩的输入。读取侧同时认 jsonl 与 proto，所以存量 .jsonl 文件不用搬。",
        "_comment_flush_interval_secs": "落盘由两个条件触发：缓冲满 max_buffer_size 条，或距上次落盘满 flush_interval_secs 秒。低流量 stream 可以长时间够不到条数阈值，那期间写下的记录全在进程内存里，进程一挂即失——所以时间上界不能省。置 0 只留条数阈值。",
        "enabled": true,
        "base_dir": "/var/lib/cogneva-data/raw-logs",
        "max_buffer_size": 1000,
        "format": "proto",
        "zstd_level": null,
        "flush_interval_secs": 1
      },
      "memory": {
        "_comment": "Memory backend — composite puts Raw on the shared ObjectBackend, Schema and the Summary entry store on PostgreSQL, and the summary vectors on the shared VectorBackend, so the dataset survives a restart. This applies to the processes that enable it: the evolution worker overrides COGNEVA_MEMORY_ENABLED to false, so it holds no memory dataset at all and any state it must keep across a restart lives in its own app.data_dir instead.",
        "_comment_models": "load_*_model gates the ONNX weights loaded at startup. Both are false on the shipped profile: the single node has ~1.8GiB free against ~2.2GiB of resident weights per session (dense and sparse each open their own session over the same BGE-M3 weights, plus the reranker), so loading either one evicts the rest of the workload. Weights are deliberately kept out of the image and out of the release archive (4.6GiB total, and gzip cannot shrink fp32 weights); they are to arrive as one authoritative copy in the object store, seeded into each node's local cache. Until a host has the RAM, the vector layer stays absent and search falls back to the schema/text path.",
        "enabled": true,
        "backend_type": "composite",
        "embedding_dimension": 1024,
        "auto_ingest": true,
        "load_embedding_model": false,
        "load_reranker_model": false,
        "ingest": {
          "_comment": "Auto-ingest pipeline knobs. Retries and the dead-letter queue apply to content-level failures (a message that cannot be extracted, however often it is retried); an unreachable upstream defers the message back to the event plane instead, so a capacity outage never decides which events enter memory.",
          "_comment_pull_gate": "The pull gate has two inputs sharing one verdict: the security gateway's published pool snapshot (cross-process, known before an attempt is spent) and this process's own consecutive environment failures (a local fact that still holds when Redis has no key or the gateway restarted). Either one pauses pulling and the wait is the larger of the two. Pausing means not pulling, so messages keep their place in the event plane without spending a delivery attempt.",
          "_comment_reconcile": "Startup reconcile only covers the window between a crash and the restart, so it also re-runs on an interval: an outage longer than the lookback would otherwise push early-archived, never-extracted raws out of the window for good. 0 keeps it startup-only.",
          "_comment_bus": "Bus-mode consumer knobs (active when multi_backend_consumer.events_on_bus=true). bus_claim_min_idle_ms must exceed the worst-case single-event processing time so a live worker's job is never claimed away; JetStream redelivery is governed by ack_wait, the claim sweeper covers Redis-class backends.",
          "max_retries": 3,
          "retry_base_delay_ms": 1000,
          "enable_dlq": true,
          "dlq_namespace": "dlq",
          "extraction_concurrency": 4,
          "startup_reconcile": true,
          "reconcile_lookback_hours": 24,
          "reconcile_interval_secs": 600,
          "pull_pause_after_failures": 3,
          "pull_pause_initial_secs": 60,
          "pull_pause_max_secs": 1800,
          "pool_check_secs": 30,
          "backlog_warn_at": 64,
          "bus_group": "memory-ingestor",
          "bus_claim_interval_secs": 30,
          "bus_claim_min_idle_ms": 900000,
          "bus_claim_batch": 32
        }
      },
      "tier_migrator": {
        "_comment": "hot→warm 就地 zstd 压缩并删除源文件，warm→cold 上传对象存储再删本地，两步都不可逆——热层保留 hot_duration_secs，warm 保留 warm_duration_secs。",
        "_comment_owner": "迁移必须只有一个属主：raw_log_index 按 (stream, log_date, format) 唯一，冷层对象键又不带进程身份，两个部署同时扫就会互相覆盖索引行与对象（谁后写谁赢，先写的那份不可找回且不报错）。属主是主应用 Pod；进化 Pod 在 app-config 里置 COGNEVA_TIER_MIGRATOR_ENABLED=false。",
        "enabled": true,
        "hot_duration_secs": 86400,
        "warm_duration_secs": 604800,
        "warm_compression_level": 3,
        "cold_compression_level": 9,
        "scan_interval_secs": 3600,
        "cold_key_prefix": "raw",
        "trace_scan_batch": 5000
      },
      "supervisor": {
        "health_interval_secs": 10,
        "quota_interval_secs": 60,
        "rebalance_interval_secs": 300,
        "event_window_secs": 30,
        "broadcast_capacity": 256,
        "quota_threshold": 1000,
        "behavior_history_max": 20,
        "heartbeat_history_max": 1000,
        "alert_history_max": 10000,
        "control_plane_interval_secs": 30,
        "control_plane_url": null,
        "health_checker": {
          "suspect_after_secs": 15,
          "dead_after_secs": 60,
          "stuck_after_secs": 600
        },
        "task_rebalancer": {
          "max_tasks_per_agent": 4,
          "max_assignments_per_pass": 32
        }
      },
      "hook_engine": {
        "dedup_window_secs": 1,
        "hook_timeout_secs": 30,
        "default_rate_limit": {
          "burst": 100,
          "per_second": 100
        }
      },
      "boundary": {
        "rules": [
          {
            "name": "TokenBudget",
            "rule_type": "hard",
            "threshold": 100000,
            "description": "单个任务的 estimated_tokens 不得超过阈值",
            "enabled": true
          },
          {
            "name": "OutputQuality",
            "rule_type": "soft",
            "threshold": null,
            "description": "生成输出必须满足以下质量标准：1) 代码必须包含单元测试 2) 文档必须完整 3) 无已知安全漏洞",
            "enabled": true
          }
        ]
      },
      "agent_loop": {
        "agent_id": "agent-001",
        "role": "planner",
        "max_iterations": 10,
        "context_window_size": 64000,
        "skill_cache_ttl_secs": 30
      },
      "observability": {
        "infra_watch": {
          "_comment": "基础设施告警→持久化→自发现。规则即部署策略：阈值改这里不用重建镜像。series 消失或条件不再成立会自动 resolve；规则查询失败不会误 resolve。",
          "enabled": true,
          "prometheus_url": "http://cog-observability-prometheus.monitoring:9090",
          "poll_interval_secs": 60,
          "eval_failure_alert_after": 3,
          "rules": [
            {
              "name": "node_disk_usage_high",
              "promql": "100 - 100 * node_filesystem_avail_bytes{mountpoint=\"/\", fstype=~\"ext4|xfs|overlay\"} / node_filesystem_size_bytes{mountpoint=\"/\", fstype=~\"ext4|xfs|overlay\"}",
              "condition": {
                "greater_than": 75.0
              },
              "severity": "warning",
              "summary": "node root filesystem usage above 75% (current {value}%)"
            },
            {
              "name": "node_disk_usage_critical",
              "promql": "100 - 100 * node_filesystem_avail_bytes{mountpoint=\"/\", fstype=~\"ext4|xfs|overlay\"} / node_filesystem_size_bytes{mountpoint=\"/\", fstype=~\"ext4|xfs|overlay\"}",
              "condition": {
                "greater_than": 85.0
              },
              "severity": "critical",
              "summary": "node root filesystem usage above 85% (current {value}%); kubelet eviction starts soon"
            },
            {
              "name": "node_disk_pressure",
              "promql": "kube_node_status_condition{condition=\"DiskPressure\", status=\"true\"}",
              "condition": {
                "greater_than": 0.0
              },
              "severity": "critical",
              "summary": "kubelet reports DiskPressure; pods are being evicted"
            },
            {
              "name": "node_memory_pressure",
              "promql": "kube_node_status_condition{condition=\"MemoryPressure\", status=\"true\"}",
              "condition": {
                "greater_than": 0.0
              },
              "severity": "warning",
              "summary": "kubelet reports MemoryPressure"
            },
            {
              "name": "pod_restarting_fast",
              "promql": "increase(kube_pod_container_status_restarts_total{namespace=\"cogneva\"}[15m])",
              "condition": {
                "greater_than": 5.0
              },
              "severity": "warning",
              "summary": "cogneva pod restarting more than 5 times in 15m ({value})"
            },
            {
              "name": "pod_oom_killed",
              "promql": "kube_pod_container_status_last_terminated_reason{namespace=\"cogneva\", reason=\"OOMKilled\"}",
              "condition": {
                "greater_than": 0.0
              },
              "severity": "critical",
              "summary": "cogneva workload container last exit was OOMKilled; memory footprint exceeds its limit"
            },
            {
              "name": "sandbox_command_oom_killed",
              "promql": "increase(sandbox_oom_kills_total[30m])",
              "condition": {
                "greater_than": 0.0
              },
              "severity": "warning",
              "summary": "The sandbox container's memory limit killed {value} command(s) in the last 30m. A child process dying here leaves the container itself running, so nothing reaches the container-level OOMKilled signal, and the build that died reads downstream as an ordinary failure with empty output; a command that keeps dying at the limit needs a bigger limit, not a different change"
            },
            {
              "name": "ralph_loop_terminations_high",
              "promql": "increase(ralph_terminations_total[30m])",
              "condition": {
                "greater_than": 20.0
              },
              "severity": "warning",
              "summary": "Ralph loops stopped {value} times in the last 30m without passing their goal; the evolution chain is burning budget instead of converging (the reason label distinguishes budget_exhausted from stagnated)"
            },
            {
              "name": "declared_failure_class_unrecorded",
              "promql": "collab_classification_unreachable",
              "condition": {
                "greater_than": 0.0
              },
              "severity": "warning",
              "summary": "A declared failure class is being announced while its series stays at zero, so the classification is being lost between the producer and the termination record; the class label names which one"
            },
            {
              "name": "self_evolution_yields_no_change",
              "promql": "increase(self_evolution_change_yield_total{outcome=~\"no_artifacts|submit_failed\"}[6h])",
              "condition": {
                "greater_than": 2.0
              },
              "severity": "warning",
              "summary": "Self-evolution chains reported success {value} time(s) in the last 6h while producing no change to land (no_artifacts: the pipeline yielded no valid diff; submit_failed: every sink rejected it); the landing channel stays empty and the loop silently stops evolving"
            },
            {
              "name": "cogneva_workload_metrics_unscraped",
              "promql": "avg_over_time((count(kube_pod_container_info{namespace=\"cogneva\", image=~\".*cogneva.*\"} and on(pod) kube_pod_status_phase{namespace=\"cogneva\", phase=\"Running\"} == 1 unless on(pod) kube_pod_owner{namespace=\"cogneva\", owner_kind=\"Job\"}) - count(up{namespace=\"cogneva\"}))[15m:1m])",
              "condition": {
                "greater_than": 0.5
              },
              "severity": "warning",
              "summary": "On average {value} running cogneva workload(s) had no Prometheus scrape target over the last 15m: their /metrics is produced but never collected, so every alert they would raise is invisible (usual causes: the workload has no Service, its Service port is not named http or metrics, or a NetworkPolicy blocks the monitoring namespace)"
            },
            {
              "name": "data_volume_over_declared_size",
              "promql": "max by (namespace, persistentvolumeclaim) (cogneva_data_volume_used_bytes) / on(namespace, persistentvolumeclaim) kube_persistentvolumeclaim_resource_requests_storage_bytes",
              "condition": {
                "greater_than": 1.0
              },
              "severity": "warning",
              "summary": "Volume holds {value}x its declared size: the workload writes past the PVC's requests.storage, and nothing enforces that request, so the declared size is not a ceiling. For a directory-backed volume the excess lands on the node filesystem and the per-volume capacity the kubelet reports is the node's, not this volume's. Either the declaration is wrong for what this deployment writes, or the growth belongs somewhere else"
            },
            {
              "name": "trace_tier_demotion_stalled",
              "promql": "(cogneva_trace_tier_overdue > 0) and (cogneva_trace_tier_overdue == (cogneva_trace_tier_overdue offset 3h))",
              "condition": {
                "greater_than": 0.0
              },
              "severity": "warning",
              "summary": "Traces past their tier's age number {value} on tier {tier}, exactly as they did 3h ago: the demotion chain is not moving them, so the hot tier keeps growing. Any non-zero count that does not come down over three scans is a move that fails every pass, not a backlog being worked through. Nothing deletes traces, so this chain is the only thing standing between a permanent trace store and the node filesystem"
            },
            {
              "name": "trace_tier_migration_stale",
              "promql": "time() - max(cogneva_trace_tier_last_pass_seconds) > 6 * max(cogneva_trace_tier_scan_interval_seconds)",
              "condition": {
                "greater_than": 0.0
              },
              "severity": "warning",
              "summary": "No trace tier migration pass has run for {value} seconds past six times the configured scan interval: the background loop has stopped, so tier demotion is not happening at all. The threshold scales with the deployment's own cadence rather than a fixed number"
            },
            {
              "name": "trace_tier_migration_failing",
              "promql": "increase(cogneva_trace_tier_pass_failures_total[1h])",
              "condition": {
                "greater_than": 0.0
              },
              "severity": "warning",
              "summary": "Trace tier migration failed to complete {value} time(s) in the last hour: the trace store could not be listed, so a pass has nothing to look at and no tier can be demoted. A pass that runs but cannot move individual entries is reported as an overdue count instead"
            },
            {
              "name": "stream_pending_unreclaimed",
              "promql": "cogneva_stream_pending_unreclaimed_oldest_idle_seconds > 2 * cogneva_stream_pending_claim_idle_seconds",
              "condition": {
                "greater_than": 0.0
              },
              "severity": "warning",
              "summary": "A message on {stream} has been pending for {value}s, past twice the idle threshold the reclaim pass takes messages back at. That pass runs every configured interval, so surviving one means the message is stranded: the task it belongs to is stuck and nothing else will move it. The threshold is a multiple of the deployment's own reclaim idle, so changing the reclaim policy changes this with it"
            },
            {
              "name": "stream_pending_measure_stale",
              "promql": "time() - max by (stream, pod) (cogneva_stream_pending_measure_last_seconds) > 6 * max by (stream, pod) (cogneva_stream_pending_measure_interval_seconds)",
              "condition": {
                "greater_than": 0.0
              },
              "severity": "warning",
              "summary": "Pending state for {stream} has not been measured for {value}s, more than six of the configured measurement intervals. The reclaim loop that publishes it is not running, so a stranded message on this stream would be invisible again — the state this measurement exists to expose"
            },
            {
              "name": "stream_pending_measure_failing",
              "promql": "increase(cogneva_stream_pending_measure_failures_total[1h])",
              "condition": {
                "greater_than": 0.0
              },
              "severity": "warning",
              "summary": "Pending state for {stream} failed to be measured {value} time(s) in the last hour. The reclaim loop is running but cannot read the queue, so its last published figures are not evidence about the present"
            }
          ]
        },
        "trace_collector": {
          "_comment": "trace 事件按 agent 在内存缓冲直到 AgentEnd 落盘；长任务无界累积会 OOM。超过预算即分块落盘（part 后缀），预算按部署内存定。",
          "buffer_max_bytes": 16777216
        },
        "data_volume_watch": {
          "_comment": "应用数据目录的占用量由写它的进程自己量成 gauge cogneva_data_volume_used_bytes 并带上卷名，规则拿它除以 kube-state-metrics 报的 requests.storage。同一份 cogneva.json 挂给多个 Pod，各自挂的卷不是同一张，所以卷名由各部署用自己的 env 指名（COGNEVA_DATA_VOLUME_CLAIM），不写在这里；claim 为空即整个观测关闭。",
          "interval_secs": 300
        }
      },
      "self_review": {
        "_comment": "PGE actor self-review quality gate. enabled=true 时 Planner/Generator/Evaluator/Moderator/Merger 的输出会经过 SelfReviewLoop 审查并用修订结果替换原始输出。",
        "enabled": true,
        "max_iterations": 2,
        "quality_threshold": 0.8,
        "spec": null,
        "best_practices": []
      },
      "pge": {
        "_comment": "PGE actor 输出契约。schemas 按 actor 名（planner/generator/evaluator/moderator/merger）配置 JSON Schema；配置后 schema 会注入 prompt 上下文并对 LLM 原始输出做校验，校验失败仅告警并回退宽松解析，不会中断流水线。默认为空 = 使用内置契约。",
        "schemas": {}
      },
      "lifecycle": {
        "heartbeat_interval_secs": 5,
        "suspect_threshold": 2,
        "dead_threshold": 5,
        "registration_ttl_secs": 60,
        "cleanup_interval_secs": 300
      },
      "tuning": {
        "stream_capacity": 256,
        "high_watermark": 1000,
        "low_watermark": 100,
        "max_summaries": 10
      },
      "prompts": {
        "_comment": "外部 prompt 模板目录，由 cogneva-prompts configmap 挂载；hot_reload 捕捉 configmap 交换（L0 配置晋级热更新通道）。",
        "dir": "/etc/cogneva/prompts",
        "hot_reload": true
      },
      "agent_pool": {
        "enabled": true,
        "worker_count": 3,
        "worker_role": "planner"
      },
      "self_evolution": {
        "_comment": "Self-evolution auto-deploy pipeline. enabled/auto_apply/auto_deploy all true means fully autonomous: LLM generates code changes, tests, commits, builds, and switches binary without human intervention. Sandbox boundary check: real apply/deploy requires a detected isolated environment (K8s Pod/container/COGNEVA_SANDBOX), sandbox_mode=true, or force_autonomous=true; otherwise the pipeline downgrades to dry-run.",
        "enabled": true,
        "auto_apply": true,
        "auto_deploy": true,
        "sandbox_mode": true,
        "force_autonomous": false,
        "manual_approve": false,
        "change_dir": "./evolution-changes",
        "binary_dir": "/opt/cogneva/bin",
        "backup_dir": "/opt/cogneva/bin/backups",
        "switch_mode": "self_exec",
        "health_check_grace_period_secs": 10,
        "health_check_interval_secs": 5,
        "health_check_max_retries": 6,
        "test_timeout_secs": 600,
        "build_timeout_secs": 1800,
        "poll_interval_secs": 60,
        "schema_repair_interval_secs": 600,
        "notify_on_success": true,
        "notify_on_failure": true,
        "artifact_evolution": {
          "_comment": "Autonomous search over the meta-learning policy's tuning parameters (min_samples, margin). Each round replays the recorded decision outcomes under every candidate parameter set and adopts one only when a two-proportion z-test says it beats what the recommendation path is currently using; the winner is saved as a new version and hot-swapped. The input is this process's own recorded results, so it runs without LLM or network access. min_trials is the floor — below it the test cannot be significant and the round writes nothing. Ownership: the policy artifact lives under this process's own data dir, so this loop needs no per-deployment owner switch.",
          "enabled": true,
          "interval_secs": 3600,
          "policy_name": "meta_learning.mode",
          "min_trials": 40,
          "min_samples_grid": [2, 3, 5, 8],
          "margin_grid": [0.0, 0.05, 0.1, 0.15, 0.2, 0.3]
        }
      },
      "multi_backend_consumer": {
        "_comment": "events_on_bus=true: AgentEnd is published only to the persistent event plane (dedicated NATS JetStream at nats_urls, decoupled from the task-queue backend); the supervisor re-injects it into the broadcast exactly once and memory-ingestor consumes from the bus with ack-after-processing. Publish-side knobs bound the in-memory buffer that covers NATS outage windows.",
        "enabled": true,
        "channel": "cogneva-events",
        "group": "cogneva-consumer",
        "retry_interval_secs": 5,
        "events_on_bus": true,
        "nats_urls": [
          "nats-service:4222"
        ],
        "publish_buffer_capacity": 256,
        "publish_retry_base_delay_ms": 1000,
        "events_stream_retention": "limits"
      },
      "github_integration": {
        "_comment": "GitHub integration for self-evolution. Tokens are resolved from environment variables and never enter the sandbox. discovery_mode=both: polling is the periodic fallback, webhook events (via the security gateway, gateway_verified) give second-level response.",
        "enabled": true,
        "_comment_discovery_enabled": "Whether THIS process runs the discovery loops. One binary serves several deployments (main app, evolution pod, security gateway, sandbox executor) and each of the first two builds its own plugin table, so without this switch both scan the same upstream items and ask the same clarification twice: intent guards are per-pod files, so neither can see the other already acted. The owner is the main app — webhook events structurally reach only it, and polling is its periodic fallback. Deployment sets this false on the evolution pod.",
        "discovery_enabled": true,
        "discovery_mode": "both",
        "repo": "hcipengm/cogneva",
        "base_branch": "main",
        "accounts": [
          {
            "kind": "bot",
            "username": "cogneva-bot",
            "name": "Cogneva Bot",
            "email": "bot@cogneva.ai",
            "token_env": "COGNEVA_GITHUB_BOT_TOKEN",
            "token": null,
            "app_installation_id": null
          },
          {
            "kind": "human",
            "username": "human-operator",
            "name": "Human Operator",
            "email": "human@example.com",
            "token_env": "COGNEVA_GITHUB_HUMAN_TOKEN",
            "token": null
          }
        ],
        "poll_interval_secs": 300,
        "max_issues_per_scan": 50,
        "webhook": {
          "_comment": "Webhook event entry, used when discovery_mode=events/both. gateway_verified=true: platform signatures are verified at the security gateway (8082); this process only verifies the gateway's internal HMAC (COGNEVA_WEBHOOK_INTERNAL_SECRET) and serves GitHub + Gitee events on the same port. Gitee events arrive at the fixed path /webhooks/gitee.",
          "port": 9091,
          "secret_env": "COGNEVA_GITHUB_WEBHOOK_SECRET",
          "path": "/webhooks/github",
          "gateway_verified": true
        },
        "git_workdir": "",
        "_comment_git_workdir": "Local git working copy the landing channel commits and pushes from. Empty derives one under the data dir (COGNEVA_DATA_DIR/git-workdir); whether changes actually go upstream is the contribution policy's decision, not a path setting. Git transport goes through the security gateway HTTPS proxy (COGNEVA_GIT_PROXY_BASE, set in deployment env); no token or deploy key lives in this pod.",
        "api_base": null,
        "_comment_api_base": "GitHub API base override. Point it at the security gateway passthrough endpoint (COGNEVA_GITHUB_API_BASE, e.g. http://cogneva-security-gateway:8081/github) so this process holds no platform token; leave null to talk to api.github.com directly with the token_env/inline token.",
        "auto_submit_fixes": true,
        "landing_policy": {
          "_comment": "Bounds what a landed change may touch, and what happens when CI on a landed commit fails. Quality is settled by the sandbox (pre-check, apply, test, release build) before the channel is called, so there is no score gate here; whether changes go upstream at all is the contribution policy's decision (auto/ask/local), so there is no on/off switch either.",
          "max_changed_lines": 200,
          "forbidden_paths": [
            ".github/workflows",
            "deploy/",
            "secrets/",
            "*.lock"
          ],
          "revert_on_ci_failure": true,
          "redrive_on_ci_failure": true,
          "ci_watch_timeout_secs": 1800,
          "ci_poll_interval_secs": 60,
          "max_land_attempts": 3
        },
        "human_required_labels": [
          "security",
          "breaking-change"
        ],
        "forbidden_labels": [
          "wontfix",
          "manual-only"
        ],
        "allowed_issue_states": [
          "open"
        ],
        "conversation": {
          "max_clarification_rounds": 3,
          "awaiting_reply_timeout_hours": 72,
          "auto_reply": true,
          "bot_signature": "— Cogneva Bot"
        },
        "bot_identity": {
          "username": "cogneva-bot",
          "name": "Cogneva Bot",
          "email": "bot@cogneva.ai"
        }
      },
      "gitee_integration": {
        "_comment": "Gitee integration, equal in status to GitHub: issues are external-intent evolution entries. Platform fields only; triage/conversation/auto-merge policies are inherited from github_integration. api_base defaults to the security gateway passthrough (COGNEVA_GITEE_API_BASE env); token lives only in the gateway.",
        "enabled": true,
        "repo": "hcipengm/cogneva",
        "base_branch": "main",
        "poll_interval_secs": 300,
        "max_issues_per_scan": 50
      }
    }
    
