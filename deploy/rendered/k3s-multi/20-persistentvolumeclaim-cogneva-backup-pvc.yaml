# GENERATED by deploy/scripts/render-deploy.sh — DO NOT EDIT.
# Source: helm chart deploy/helm/cogneva + profiles/k3s-multi.yaml
# Change chart templates / values / profile, then re-run render-deploy.sh.
# Source: cogneva/templates/backup.yaml
# 数据面备份：权威数据（PG 逻辑导出 + Qdrant 一致性快照 + 对象 raw 层 +
# 实例身份 Secret）每日打成 tar.zst 落专用备份卷。重启档由 PVC keep 策略
# 覆盖，本备份覆盖重装/换机档（restore-from-package.sh 消费）。
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cogneva-backup-pvc
  namespace: cogneva
  labels:
    app.kubernetes.io/name: cogneva
    app.kubernetes.io/component: backup
  annotations:
    # 与数据卷同级待遇：卸载不销毁，重装后备份还在原地可恢复。
    helm.sh/resource-policy: keep
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
